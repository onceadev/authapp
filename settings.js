import React from 'react';
import {Text, View, StyleSheet, ImageBackground, TouchableOpacity, TouchableOpacityBase} from 'react-native';

function Settings({navigation}) {
    return(
        <View><Text>Settings</Text></View>
    )
}

export default Settings;